import { Layout } from '../../components/Layout';
import { TimelineForm } from '../../components/TimelineForm';

export const TimelinePickerScreen = () => (
  <Layout>
    <TimelineForm />
  </Layout>
);
